DATABASE_URL="postgres://benisploy:benisploy-dev@localhost:5432/benisploy"
REDIS_URL="redis://localhost:6389"
DEPLOY_SCRIPTS_DIR="../../deploy/node-setup"
# SSH private key encryption (AES-256-GCM)
# Generate with: openssl rand -hex 32
# Leave empty to disable encryption (dev only)
# ENCRYPTION_KEY=""
# OAuth state store (one-time GitHub callback anti-forgery tokens).
# Optional in dev (falls back to an in-memory store); required in production.
# REDIS_URL="redis://localhost:6389"

