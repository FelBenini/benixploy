# Deploy

Deployment assets for the control plane.

- `docker-compose.yml` — one-command deploy of the control plane (web app + Postgres)
- `install.sh` — one-line curl-pipe install script for the node agent (future)

See the [roadmap](../docs/ROADMAP.md) for the deployment target.
